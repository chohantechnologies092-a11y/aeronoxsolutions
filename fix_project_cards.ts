import { db } from './src/lib/firebase-admin'; async function updateProjects() { const projects = db.collection('projects'); const hadafSnapshot = await projects.where('slug', '==', 'hadaf-al-muteena').get(); hadafSnapshot.docs.forEach(doc => { doc.ref.update({ tags: 'Web Development', description: 'A comprehensive travel agency platform with online visa processing and travel packages.' }); }); const excellencySnapshot = await projects.where('slug', '==', 'excellency-beds').get(); excellencySnapshot.docs.forEach(doc => { doc.ref.update({ tags: 'Web Development', description: 'A luxurious e-commerce showcase for premium quality beds and mattresses.' }); }); console.log('Successfully updated tags and descriptions'); } updateProjects().catch(console.error);
