import { db } from './src/lib/firebase-admin'; async function updateSEOImage() { const snapshot = await db.collection('services').where('title', '==', 'Search Engine Optimization').get(); if (snapshot.empty) { console.log('Service not found'); return; } for (const doc of snapshot.docs) { await doc.ref.update({ image: '/images/services/technical_seo_1783929401183.png' }); console.log('Successfully updated image for Search Engine Optimization'); } } updateSEOImage().catch(console.error);
