import { db } from './src/lib/firebase-admin'; async function removeProjects() { const projects = db.collection('projects'); const hadafSnapshot = await projects.where('slug', '==', 'hadaf-al-muteena').get(); hadafSnapshot.docs.forEach(doc => { doc.ref.delete(); }); const excellencySnapshot = await projects.where('slug', '==', 'excellency-beds').get(); excellencySnapshot.docs.forEach(doc => { doc.ref.delete(); }); console.log('Successfully deleted projects'); } removeProjects().catch(console.error);
