import { db } from './src/lib/firebase-admin'; async function fixData() { const snapshot = await db.collection('projects').get(); for (const doc of snapshot.docs) { const data = doc.data(); if (data.slug === 'perfumish') { await doc.ref.update({ category: 'SEO & Marketing' }); } if (data.slug === 'ducting-delivered') { await doc.ref.delete(); } } console.log('Fixed'); } fixData();